

import 'package:flutter/cupertino.dart';

class TaskProvider {

  // final TaskRepository _taskRepository;   List<Task> _tasks = [];      TaskProvider() : _taskRepository = locator<TaskRepository>();

}